# Python
#
local branch
    git branch
latest changes
    git status
to push the changes
    git push origin filename
to stage the changes
    git add file name
to see single file
    git diff file name
to create a branch  
    git checkout -b branch name
to commit changes
    git commit -m masg















